# Point at your ABP host + auth server. Copy to `.env.local`.
VITE_API_URL=https://localhost:44305
VITE_AUTH_URL=https://localhost:44305
VITE_CLIENT_ID=LoopConsole_App

# Serve the bundled mock dataset instead of calling the API.
# Set to "false" once the backend is reachable.
VITE_USE_MOCKS=true
