name: plex
description: PLEX is Flutter UI framework for enterprise apps with pre-built components and best practices for efficient development in addition with many built in widgets

version: 0.6.9
homepage: https://github.com/rehmnabdul/plex

environment:
  sdk: '>=3.0.0 <4.0.0'
  flutter: ">=1.17.0"

platforms:
  android:
  ios:
  linux:
  macos:
  web:
  windows:

dependencies:
  flutter:
    sdk: flutter

  #Connectivity
  connectivity_plus: ^6.1.0

  #Navigation
  get: ^4.6.6

  # Messages
  toastification: ^2.3.0

  #Path
  path_provider: ^2.1.5

  #Storage
  shared_preferences: ^2.3.3

  #Networking
  http: ^1.2.2
  cached_network_image: ^3.4.1

  #Utils
  intl: ^0.19.0

  #Animations
  flutter_animate: ^4.5.1

  #Table
  syncfusion_flutter_core: ^27.2.3
  syncfusion_flutter_datagrid: ^27.2.3
  syncfusion_flutter_datagrid_export: ^27.2.3

  #Print Excel File
  syncfusion_flutter_xlsio: ^27.2.3
  file_saver: ^0.2.14

  #Code Builder
  build: ^2.4.1
  source_gen: ^1.5.0
  analyzer: ^6.4.1

  #Database
  sembast: ^3.7.1+3

dev_dependencies:
  flutter_lints: ^4.0.0
  build_runner: ^2.4.11

flutter:

  assets:
    - packages/plex/assets/excel.png
    - packages/plex/assets/pdf.png
    - packages/plex/assets/group_data.png